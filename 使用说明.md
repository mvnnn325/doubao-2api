# 🎯 doubao-2api 真·单文件版本 - 中文使用说明

## 📖 简介

这是一个**完全自包含的单文件 Deno 应用**，将豆包（doubao.com）转换为 OpenAI 兼容的 API 服务。

**核心特点**：
- ✅ **只需一个文件**：所有代码和配置都在 `main.ts` 中
- ✅ **无需 .env 文件**：配置直接在代码顶部修改
- ✅ **零外部依赖文件**：真正的单文件架构
- ✅ **开箱即用**：修改配置后一条命令启动
- ✅ **配置超简单**：只需要 Cookie，无需其他复杂参数

---

## 🚀 三步快速开始

### 第 1 步：安装 Deno

```bash
# Linux / macOS 用户
curl -fsSL https://deno.land/install.sh | sh

# macOS 用户（使用 Homebrew）
brew install deno

# Windows 用户（使用 PowerShell）
irm https://deno.land/install.ps1 | iex
```

安装完成后验证：
```bash
deno --version
```

---

### 第 2 步：配置 main.ts

用任何文本编辑器（VSCode、Sublime、Notepad++ 等）打开 `main.ts`，找到**第 24-62 行**的配置区域：

```typescript
const CONFIG = {
  // ========================================
  // 👇 请修改以下配置
  // ========================================
  
  // 1️⃣ API 访问密钥（建议修改为复杂密钥）
  API_MASTER_KEY: "sk-doubao-2api-your-secret-key-please-change-me",
  
  // 2️⃣ 服务端口号
  PORT: 8088,
  
  // 3️⃣ 豆包 Cookie（必须配置）
  DOUBAO_COOKIES: [
    "在这里粘贴您的 Cookie 字符串",
    // 可以添加更多账号（可选）
  ],
  
  // 4️⃣ 其他可选配置
  SESSION_CACHE_TTL: 3600,
  API_REQUEST_TIMEOUT: 180000,
  DEFAULT_MODEL: "doubao-pro-chat",
  MODEL_MAPPING: {
    "doubao-pro-chat": "7338286299411103781",
  },
};
```

#### 🍪 获取 Cookie

这是唯一需要的关键配置！请仔细按照以下流程操作：

**第 1 步**：打开豆包网站
- 访问：https://www.doubao.com/chat/
- 确保您已登录

**第 2 步**：打开开发者工具
- 按键盘上的 `F12` 键
- 或右键点击页面 → 选择"检查"

**第 3 步**：切换到网络面板
- 在开发者工具顶部找到 **"Network"（网络）** 标签
- 点击进入

**第 4 步**：发送测试消息
- 在豆包聊天界面随便输入一条消息，例如："你好"
- 点击发送

**第 5 步**：找到 completion 请求
- 在网络请求列表中，找到名为 **`completion`** 的请求
- 点击这个请求

**第 6 步**：复制 Cookie
- 在右侧面板中，找到 **"Headers"（请求头）** 标签
- 向下滚动，找到 **"Request Headers"（请求头）** 部分
- 找到 **`Cookie:`** 字段
- 鼠标选中完整的 Cookie 值，右键复制
- 粘贴到 `CONFIG.DOUBAO_COOKIES` 数组中

**完成配置示例**：
```typescript
const CONFIG = {
  API_MASTER_KEY: "sk-my-super-secret-key-2025",
  PORT: 8088,
  DOUBAO_COOKIES: [
    "_ga=GA1.1.xxx; flow_user_country=CN; s_v_web_id=verify_xxx; ...",
  ],
  SESSION_CACHE_TTL: 3600,
  API_REQUEST_TIMEOUT: 180000,
  DEFAULT_MODEL: "doubao-pro-chat",
  MODEL_MAPPING: {
    "doubao-pro-chat": "7338286299411103781",
  },
};
```

---

### 第 3 步：运行服务

在终端中运行以下命令：

```bash
deno run --allow-net --allow-read main.ts
```

**权限说明**：
- `--allow-net`：允许网络访问（连接豆包服务器）
- `--allow-read`：允许读取文件（Playwright 需要）

**启动成功标志**：

看到以下输出表示服务已成功启动：

```
╔════════════════════════════════════════════════════════════════╗
║            doubao-2api - Deno 单文件版本                       ║
║    将 doubao.com 转换为 OpenAI 兼容 API 的高性能代理         ║
╚════════════════════════════════════════════════════════════════╝

✅ 配置验证通过，加载了 1 个凭证
🚀 应用启动中... doubao-2api v1.0.0-deno
🔑 凭证管理器已初始化，共加载 1 个凭证
💾 会话管理器已初始化，缓存 TTL: 3600 秒
🚀 正在初始化 Playwright 管理器 (签名服务模式)...
...
✅ 服务器已启动在 http://0.0.0.0:8088
```

---

## 🧪 测试服务

### 测试 1：健康检查

打开新的终端窗口，运行：

```bash
curl http://localhost:8088/
```

**预期响应**：
```json
{
  "message": "欢迎来到 doubao-2api v1.0.0-deno. 服务运行正常。"
}
```

### 测试 2：获取模型列表

```bash
curl http://localhost:8088/v1/models \
  -H "Authorization: Bearer sk-my-super-secret-key-2025"
```

**预期响应**：
```json
{
  "object": "list",
  "data": [
    {
      "id": "doubao-pro-chat",
      "object": "model",
      "created": 1234567890,
      "owned_by": "lzA6"
    }
  ]
}
```

### 测试 3：聊天请求（流式）

```bash
curl http://localhost:8088/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer sk-my-super-secret-key-2025" \
  -d '{
    "model": "doubao-pro-chat",
    "messages": [{"role": "user", "content": "你好，请介绍一下自己"}],
    "stream": true
  }'
```

如果看到流式返回的文本内容，说明一切正常！🎉

---

## 🐍 在 Python 中使用

### 安装 OpenAI SDK

```bash
pip install openai
```

### 示例代码

```python
from openai import OpenAI

# 创建客户端
client = OpenAI(
    base_url="http://localhost:8088/v1",
    api_key="sk-my-super-secret-key-2025"  # 使用您配置的密钥
)

# 发送聊天请求（流式）
response = client.chat.completions.create(
    model="doubao-pro-chat",
    messages=[
        {"role": "user", "content": "你好，请介绍一下你自己"}
    ],
    stream=True
)

# 打印流式响应
for chunk in response:
    if chunk.choices[0].delta.content:
        print(chunk.choices[0].delta.content, end="", flush=True)

print("\n")
```

---

## 📱 在其他语言中使用

### Node.js / TypeScript

```typescript
import OpenAI from 'openai';

const client = new OpenAI({
  baseURL: 'http://localhost:8088/v1',
  apiKey: 'sk-my-super-secret-key-2025',
});

const response = await client.chat.completions.create({
  model: 'doubao-pro-chat',
  messages: [{ role: 'user', content: '你好' }],
  stream: true,
});

for await (const chunk of response) {
  process.stdout.write(chunk.choices[0]?.delta?.content || '');
}
```

---

## ❓ 常见问题解答

### Q1：为什么启动失败提示 Cookie 未配置？

**原因**：`CONFIG.DOUBAO_COOKIES` 数组为空或 Cookie 无效。

**解决方法**：
1. 确保在 `CONFIG.DOUBAO_COOKIES` 数组中添加了 Cookie
2. Cookie 必须用双引号 `"` 包裹
3. 不要保留占位符文本（"在此处粘贴..."）
4. 按照"第 2 步"重新获取 Cookie

---

### Q2：请求失败，返回 401 Unauthorized

**原因**：API Key 不匹配。

**解决方法**：
- 确保请求头中的 API Key 与 `CONFIG.API_MASTER_KEY` 一致
- 或者将 `API_MASTER_KEY` 设置为 `"1"` 来禁用认证：
  ```typescript
  const CONFIG = {
    API_MASTER_KEY: "1",  // 禁用认证
    // ...
  };
  ```

---

### Q3：服务启动后但请求无响应

**可能原因**：
1. Cookie 已过期
2. IP 被豆包限制

**解决方法**：
1. 按照"第 2 步"重新获取最新的 Cookie
2. 更换网络环境（换个 WiFi 或使用移动数据）
3. 等待一段时间后重试

---

### Q4：如何添加多个豆包账号？

**方法**：在 `CONFIG.DOUBAO_COOKIES` 数组中添加多个 Cookie：

```typescript
const CONFIG = {
  DOUBAO_COOKIES: [
    "第一个账号的完整 Cookie 字符串",
    "第二个账号的完整 Cookie 字符串",
    "第三个账号的完整 Cookie 字符串",
  ],
  // ...
};
```

系统会自动在多个账号之间轮询，提高并发能力。

---

### Q5：如何更改服务端口？

**方法**：修改 `CONFIG.PORT`：

```typescript
const CONFIG = {
  PORT: 3000,  // 改为您想要的端口
  // ...
};
```

---

### Q6：Cookie 多久需要更新一次？

**答案**：通常 Cookie 的有效期为 **7-30 天**，建议：
- 每周检查一次服务是否正常
- 如果出现请求失败，立即更新 Cookie
- 可以设置定时提醒

---

### Q7：如何在后台运行服务？

**方法 1：使用 nohup**
```bash
nohup deno run --allow-net --allow-read main.ts > app.log 2>&1 &
```

**方法 2：使用 screen**
```bash
screen -S doubao
deno run --allow-net --allow-read main.ts
# 按 Ctrl+A 然后按 D 退出
# 重新连接：screen -r doubao
```

**方法 3：使用 systemd（推荐生产环境）**

创建 `/etc/systemd/system/doubao-2api.service`：
```ini
[Unit]
Description=doubao-2api Service
After=network.target

[Service]
Type=simple
User=your-username
WorkingDirectory=/path/to/your/project
ExecStart=/path/to/deno run --allow-net --allow-read /path/to/main.ts
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

启动服务：
```bash
sudo systemctl daemon-reload
sudo systemctl enable doubao-2api
sudo systemctl start doubao-2api
```

---

## 🔒 安全建议

1. **修改默认密钥**：`API_MASTER_KEY` 应设置为强密码
2. **使用 HTTPS**：生产环境建议使用 Nginx 反向代理
3. **限制访问来源**：配置防火墙规则
4. **不要公开分享**：包含 Cookie 的文件不要上传到公共位置
5. **定期更新**：定期更新 Cookie

---

## 📊 性能建议

| 建议 | 说明 |
|------|------|
| **多账号轮询** | 配置 2-3 个豆包账号，提高并发能力 |
| **合理设置超时** | 根据网络情况调整 `API_REQUEST_TIMEOUT` |
| **使用反向代理** | Nginx 可以提供缓存、负载均衡等功能 |
| **监控资源** | Playwright 占用约 200-500MB 内存 |
| **定期重启** | 建议每周重启一次服务 |

---

## 🛠️ 故障排查

### Playwright 初始化失败

**现象**：启动时卡在 "正在初始化 Playwright 管理器"

**原因**：首次运行时需要下载 Chromium 浏览器（约 200MB）

**解决方法**：
```bash
# 手动安装 Chromium
npx playwright install chromium

# Linux 服务器还需要安装依赖
sudo apt-get update
sudo apt-get install -y libnss3 libatk1.0-0 libcups2 libdrm2 \
  libxkbcommon0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 \
  libgbm1 libasound2 libpango-1.0-0 libcairo2
```

---

### 内存不足

**现象**：服务运行一段时间后崩溃

**原因**：Playwright 需要较多内存

**解决方法**：
- 确保系统有至少 1GB 可用内存
- 配置 swap 交换空间
- 减少并发请求数量

---

## 🎓 更多资源

- **快速开始**：[QUICKSTART.md](./QUICKSTART.md)
- **完整文档**：[README_SINGLE_FILE.md](./README_SINGLE_FILE.md)
- **Deno 官网**：https://deno.land/
- **OpenAI API 文档**：https://platform.openai.com/docs/api-reference

---

## 🎉 祝您使用愉快！

如果遇到问题，请仔细阅读本文档的"常见问题解答"部分。

**记住**：这是一个**超简单的单文件**应用，只需要配置 Cookie 就能运行！ ✨
